{
  "name": "my-vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "install:browsers": "playwright install --with-deps chromium",
    "test:playwright": "playwright test",
    "check:all": "start-server-and-test dev http://localhost:5173 test:playwright"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "playwright": "^1.56.1",
    "vite": "^7.2.4",
    "wait-on": "^9.0.3",
    "start-server-and-test": "^1.14.0"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
